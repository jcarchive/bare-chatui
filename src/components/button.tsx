import {MouseEventHandler, ReactElement, useState} from "react";

export function MyButton(props: { text: string }): ReactElement {
    return (
        <section>
            <textarea/>
            <button type={"button"}/>
        </section>
    )
}
